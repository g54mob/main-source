## CustomTextEditorSystem(with SysntaxBaseHighlighter) with LoopLanguageSystem.
- (optional) Camera(component). usePostProcessing -> on.

## DoorBaseSystem
- in DoorInteraction_1 reference the fpCam to scene cam.
- Layer Door -- change --> layer name(@init of unity project).
- Elevator Slider Door (add cube outside alng with cube inside).
- (optional) Camera(component). usePostProcessing -> on.