using System;
using System.Collections.Generic;
using Player.GameplayInput.ButtonsActions;

public interface mo<a, b> : mm where a : Enum where b : ButtonActionData
{
	event Action<b> qpf;

	event Action<b> qpg;

	mn fmh(a a);

	bool fmi(a a, out lp b);

	b fmj(a a);

	IEnumerable<b> fmk();
}
