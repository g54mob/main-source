using UnityEngine;
using UnityEngine.Audio;

public interface bhn
{
	void ixy(AudioResource a, float? b = null, float? c = null, float d = 0f, float e = 1f, float f = 500f, bool g = true, Vector3 h = default(Vector3), Quaternion i = default(Quaternion), Transform j = null, bool k = true);
}
