using Unity.Mathematics;
using VoxelMeshGeneration;

public sealed class tr
{
	private readonly int3 rrr;

	private readonly float rrs;

	private readonly byte rrt;

	public hx<int3, VoxelMesh.Voxel> guc(hx<int3, VoxelMesh.Voxel> a)
	{
		return null;
	}

	private RGBAtlasColor mvs(int3 a, RGBAtlasColor b, ce.MapType c)
	{
		return default(RGBAtlasColor);
	}

	public tr(int3 a, float b, byte c)
	{
	}

	private hx<int3, VoxelMesh.Voxel> fqs(hx<int3, VoxelMesh.Voxel> a)
	{
		return null;
	}

	private hx<int3, VoxelMesh.Voxel> gud(hx<int3, VoxelMesh.Voxel> a)
	{
		return null;
	}

	private RGBAtlasColor nxi(int3 a, RGBAtlasColor b, ce.MapType c)
	{
		return default(RGBAtlasColor);
	}

	private RGBAtlasColor cmo(int3 a, RGBAtlasColor b, ce.MapType c)
	{
		return default(RGBAtlasColor);
	}

	private RGBAtlasColor ewi(int3 a, RGBAtlasColor b, ce.MapType c)
	{
		return default(RGBAtlasColor);
	}

	private RGBAtlasColor gue(int3 a, RGBAtlasColor b, ce.MapType c)
	{
		return default(RGBAtlasColor);
	}
}
