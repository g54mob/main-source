using System;

namespace Gh.Tk
{
	public static class ActivityPickUp
	{
		public static Activity CreatePickUpActivity(Actor actor, GameItem itemToPickUp, Inventory inventory = null, bool removeFromInventory = true, int position = -1, string usage = "pickup", Action finishAction = null)
		{
			return null;
		}
	}
}
