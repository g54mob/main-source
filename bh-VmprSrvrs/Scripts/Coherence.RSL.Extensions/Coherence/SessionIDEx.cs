using Coherence.Brook;
using Coherence.Log;
using Coherence.Transport;

namespace Coherence
{
	public static class SessionIDEx
	{
		public static SessionID ExtractSessionID(IInOctetStream stream, Logger logger)
		{
			return default(SessionID);
		}
	}
}
