using System.Collections.Generic;
using Noesis;
using UnityEngine;

internal class NoesisKeyCodes
{
	private static Dictionary<KeyCode, Key> _unityToNoesis;

	static NoesisKeyCodes()
	{
	}

	public static Key Convert(KeyCode key)
	{
		return default(Key);
	}
}
