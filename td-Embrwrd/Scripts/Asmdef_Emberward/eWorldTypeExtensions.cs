using UnityEngine;

public static class eWorldTypeExtensions
{
	public static eWorldType GetPreviousWorld(this eWorldType worldType)
	{
		return default(eWorldType);
	}

	public static int ToIndex(this eWorldType worldType)
	{
		return 0;
	}

	public static Color GetWorldColor(this eWorldType worldType)
	{
		return default(Color);
	}
}
