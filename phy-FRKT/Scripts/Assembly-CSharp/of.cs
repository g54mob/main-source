using System.Collections.Generic;
using Infrastructure.Project.Services.Tags;
using UnityEngine;
using UnityEngine.EventSystems;

public class of : og
{
	private readonly nb qub;

	private readonly ok quc;

	private readonly bfn qud;

	public Ray jqs()
	{
		return default(Ray);
	}

	public bool fte(out RaycastHit a, float b = 1000f, int c = -1)
	{
		a = default(RaycastHit);
		return false;
	}

	public bool ftf(out Rigidbody a, out RaycastHit b, float c = 1000f, int d = -1)
	{
		a = null;
		b = default(RaycastHit);
		return false;
	}

	public Ray epn()
	{
		return default(Ray);
	}

	public Ray col()
	{
		return default(Ray);
	}

	public Ray fth()
	{
		return default(Ray);
	}

	public of(nb a, ok b, bfn c)
	{
	}

	public bool ftg(out List<RaycastResult> a, ObjectTag? b = null)
	{
		a = null;
		return false;
	}

	public Ray kcl()
	{
		return default(Ray);
	}
}
