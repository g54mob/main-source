using Infrastructure.Project.Registration;
using LVA.Limbs;
using Unity.Collections;
using UnityEngine;

public interface bie
{
	a izf<a>(PrefabID a, Vector3 b, Quaternion c, Transform d, Vector3 e) where a : AbstractLimb;

	b izg<b>(PrefabID a, Vector3 b, Quaternion c, bam d, NativeHashSet<int> e, Vector3 f) where b : AbstractLimb;

	void izh(AbstractLimb a);
}
