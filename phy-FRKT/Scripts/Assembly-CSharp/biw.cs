using VoxelMeshGeneration;

public interface biw
{
	VoxelMesh wtb { get; }

	bool cyl<a>(bjb<a> a, out bje b) where a : biu;

	bool cym<b>(bjb<b> a, out bje b) where b : biu;

	void cyn<c>(bjb<c> a) where c : biu;
}
