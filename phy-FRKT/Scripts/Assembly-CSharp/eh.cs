using Unity.Collections;
using Unity.Mathematics;
using VoxelMeshGeneration;
using VoxelMeshGeneration.Chunks;
using VoxelMeshGeneration.DataBuilding;

public static class eh
{
	private static void dut(float3 a, RGBAtlasColor b, float3 c, int d, NativeArray<VoxelMeshVertex> e, int f)
	{
	}

	private static void dpu(NativeArray<ushort> a, ushort b, ushort c)
	{
	}

	public static VoxelMeshData.Counter dpo(VoxelMesh.Voxels a, float3 b, VoxelMeshChunkData c, VoxelMeshData d)
	{
		return default(VoxelMeshData.Counter);
	}

	public static VoxelMeshData.Counter fyr(VoxelMesh.Voxels a, float3 b, VoxelMeshChunkData c, VoxelMeshData d)
	{
		return default(VoxelMeshData.Counter);
	}

	private static void mvk(NativeArray<ushort> a, ushort b, ushort c)
	{
	}

	private static float2 gda(RGBAtlasColor a)
	{
		return default(float2);
	}

	private static VoxelMeshData msa(VoxelMesh.Voxels a, float3 b, int3 c, VoxelMeshData d)
	{
		return default(VoxelMeshData);
	}

	public static VoxelMeshData.Counter kku(VoxelMesh.Voxels a, float3 b, VoxelMeshChunkData c, VoxelMeshData d)
	{
		return default(VoxelMeshData.Counter);
	}

	private static void jew(float3 a, RGBAtlasColor b, float3 c, int d, NativeArray<VoxelMeshVertex> e, int f)
	{
	}

	private static bool ibt(VoxelMesh.Voxels a, int3 b, int3 c)
	{
		return false;
	}

	private static float2 bqk(RGBAtlasColor a)
	{
		return default(float2);
	}

	private static VoxelMeshData.Counter dpp(VoxelMeshData.Counter a)
	{
		return default(VoxelMeshData.Counter);
	}

	private static VoxelMeshData dof(VoxelMesh.Voxels a, float3 b, int3 c, VoxelMeshData d)
	{
		return default(VoxelMeshData);
	}

	private static float2 erf(RGBAtlasColor a)
	{
		return default(float2);
	}

	public static VoxelMeshData.Counter eed(VoxelMesh.Voxels a, float3 b, VoxelMeshChunkData c, VoxelMeshData d)
	{
		return default(VoxelMeshData.Counter);
	}

	private static float2 dpt(RGBAtlasColor a)
	{
		return default(float2);
	}

	private static bool nkt(VoxelMesh.Voxels a, int3 b, int3 c)
	{
		return false;
	}

	private static bool fey(VoxelMesh.Voxels a, int3 b, int3 c)
	{
		return false;
	}

	private static bool dpr(VoxelMesh.Voxels a, int3 b, int3 c)
	{
		return false;
	}

	private static VoxelMeshData nbm(VoxelMesh.Voxels a, float3 b, int3 c, VoxelMeshData d)
	{
		return default(VoxelMeshData);
	}

	private static void dps(float3 a, RGBAtlasColor b, float3 c, int d, NativeArray<VoxelMeshVertex> e, int f)
	{
	}

	private static void bq(NativeArray<ushort> a, ushort b, ushort c)
	{
	}

	private static float2 vf(RGBAtlasColor a)
	{
		return default(float2);
	}

	private static VoxelMeshData dpq(VoxelMesh.Voxels a, float3 b, int3 c, VoxelMeshData d)
	{
		return default(VoxelMeshData);
	}
}
