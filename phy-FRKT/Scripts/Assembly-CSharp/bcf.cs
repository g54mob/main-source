public interface bcf
{
	bool iej<a, b>(bcx a) where a : bdb where b : bdb;

	bool iek<c, d>(bcx a) where c : bdb where d : bdb;

	bool iel<e, f>(bcx a, bcx b, bjz c) where e : bdb where f : bdb;

	void iem(bcx a);

	void ien(bcx a, bcx b);
}
