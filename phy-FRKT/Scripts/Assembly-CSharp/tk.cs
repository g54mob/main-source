using Data.CustomTypes.LimitedValue.Dependecies.Solvers;

public class tk : bda
{
	public tk(float a, float b, float c = -1f / 0f, DependencySolverType d = DependencySolverType.TakeSmallestValue)
		: base(default(DependencySolverType), 0f, 0f, 0f)
	{
	}
}
