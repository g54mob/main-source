using UnityEngine;

public class oi : oh
{
	private readonly ok que;

	public Vector2 xcs => default(Vector2);

	public oi(ok a)
	{
	}

	public bool ftj(out RaycastHit a, float b = 1000f, int c = -1)
	{
		a = default(RaycastHit);
		return false;
	}

	public Ray ftk()
	{
		return default(Ray);
	}
}
