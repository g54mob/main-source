using System;
using Player.GameplayInput.ButtonsActions;

public interface mr<a, b, c> : mq<b, c> where a : Enum where b : Enum where c : ButtonActionData
{
	a fms(b a);

	a fms(lo a);

	void fna();
}
