using JetBrains.Annotations;

public interface bce
{
	bool ieg<a>([CanBeNull] out bjs readonlyExternalParameter) where a : bda;

	bool ieh<b>(b a, object b) where b : bda;

	bool iei<c>() where c : bda;
}
