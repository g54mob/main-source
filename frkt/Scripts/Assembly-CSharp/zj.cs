using System.Collections.Generic;
using VoxelMeshGeneration;

public class zj
{
	public VoxelMesh.Voxels hul(IReadOnlyList<rx> a, int b, out zg c)
	{
		c = null;
		return default(VoxelMesh.Voxels);
	}

	private VoxelMesh.Voxels hum(zg a, int b, int c, int d)
	{
		return default(VoxelMesh.Voxels);
	}

	private VoxelMesh.Voxels bjo(zg a, int b, int c, int d)
	{
		return default(VoxelMesh.Voxels);
	}

	private VoxelMesh.Voxels ha(zg a, int b, int c, int d)
	{
		return default(VoxelMesh.Voxels);
	}

	private VoxelMesh.Voxels mug(zg a, int b, int c, int d)
	{
		return default(VoxelMesh.Voxels);
	}

	private VoxelMesh.Voxels gle(zg a, int b, int c, int d)
	{
		return default(VoxelMesh.Voxels);
	}
}
