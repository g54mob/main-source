using System;
using Effectors.ReceiveMethods.Index;
using TripledoseLibs.Collections;

public static class bjf
{
	public static ReadOnlyFastHashSet<T> jby<T>(this FastHashSet<T> a) where T : struct, IEquatable<T>
	{
		return default(ReadOnlyFastHashSet<T>);
	}
}
